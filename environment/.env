# Base .env Configuration Template
#
# This file contains shared configurations for the project, applicable across all environments.
# Any subsequent `.env.[ENVIRONMENT]` files should safely extend these configurations without overriding unique values.
#
# IMPORTANT:
# - Values defined here are unique and should NOT be overridden in other `.env.*` files.
# - If a value needs to differ per environment, it SHOULD be moved out from here and defined in each `.env.[ENVIRONMENT]` file.
# - This ensures clarity that such values are environment-specific and not universally unique.

# =========================================
# Application Top-Level Configurations
# =========================================

# TODO: Configure your CI pipeline to auto-update `APP_VERSION` based on Git tags upon tag creation.
APP_VERSION="LATEST"  # e.g., "1.0.0", "v2.1.3"

APP_NAME="Your Application Name"
APP_SUMMARY="A brief summary of your application module."

# NOTE: Supports markdown for rich text formatting.
APP_DESCRIPTION="Detailed description of your application. This should provide an overview of its functionality, endpoints, and any other pertinent information."

APP_LICENSE_NAME="Your License Name"
APP_LICENSE_URL="https://your-repo-url.com/path-to-license"

APP_CONTACT_NAME="Your Team or Contact Name"
APP_CONTACT_URL="https://your-wiki-or-documentation-url.com"
APP_CONTACT_EMAIL="your-team-email@yourdomain.com"

OPENAPI_VERSION="3.1.0"  # Specify the OpenAPI specification version you are using.

# =========================================
# CORS (Cross-Origin Resource Sharing) Configurations
# =========================================

# Define the origins that are allowed to access your API.
APP_WEB_CORS_ALLOW_ORIGINS="*"  # e.g., "https://example.com, https://anotherdomain.com"

# Determine if credentials (such as cookies, authorization headers) are allowed.
APP_WEB_CORS_ALLOW_CREDENTIALS=True  # Set to `False` if credentials should not be allowed.

# Specify the HTTP methods that are allowed.
APP_WEB_CORS_ALLOW_METHODS="*"  # e.g., "GET, POST, PUT, DELETE"

# Specify the HTTP headers that are allowed.
APP_WEB_CORS_ALLOW_HEADERS="*"  # e.g., "Content-Type, Authorization"

# =========================================
# Secret Management Configurations
# =========================================

# Configuration for caching secrets fetched from Google Cloud Secret Manager.
GCP_SECRET_MANAGER_CACHE_TTL_SECS=300  # Time-to-live for cached secrets in seconds.

# =========================================
# Additional Configuration Variables
# =========================================

# Add any other environment-specific variables below.
# Ensure that sensitive information is managed securely and not hard-coded here.

# EXAMPLE:
# DATABASE_URL="postgres://user:password@host:port/dbname"
# REDIS_URL="redis://:password@host:port/0"

